# フェーズB 現時点の未実装事項（2026-05-04）

本メモは、`rp_ui_and_simulation_vnext.md` の方針を踏まえたうえで、2026-05-04 時点で「今回あえて未実装」とした項目を残す。

---

## 1. スコープ上の未実装（意図的）

### 1-1. LLM がシミュレーション変数を直接変更する機構

- **未実装理由**: 再現性・検証性を優先し、判定ロジックを Python に固定するため。
- **現状**:
  - 判定・メトリクス更新: `risk_simulator.py`
  - LLM: チャット説明と中止後のサポート文
- **将来検討**:
  - 「LLM 提案 -> Python の定義済み係数にマップ」の間接方式
  - 直接代入は禁止（監査困難になるため）

### 1-2. 判断イベントの優先度・抑制ルールの高度化

- **未実装理由**: 現在は可読性重視の単純ルール（フラグ切替 / max_steps）で運用しているため。
- **現状**:
  - `judgment_trigger_reason` は単一理由を返す
  - 同時発火時の微調整（クールダウンや重み付け）は未導入
- **将来検討**:
  - 理由の優先度テーブル化
  - 連続発火時の抑制（N ステップ以内は再通知しない等）

### 1-3. グラフの「完全差分描画」最適化

- **未実装理由**: まずは表示欠落解消と rebuild 抑制を優先したため。
- **現状**:
  - ドメイン固定化により不要な rebuild を抑止
  - さらなる細粒度最適化（データセット単位更新計測）は未実施
- **将来検討**:
  - パフォーマンスプロファイル計測に基づく最適化

---

## 2. 仕様として保留している項目

### 2-1. 続行時しきい値緩和の上限・減衰

- **現状仕様**: 続行のたびに `gap_danger_threshold += 0.1`（上限 1.0 へクランプ）
- **保留理由**: デモ段階では挙動理解を優先し、最適な減衰/上限設計を保留
- **将来検討**:
  - ステップ経過で閾値を徐々に戻す
  - プリセット別の緩和幅

### 2-2. 中止時のメトリクス補正のチューニング

- **現状仕様**: `fatigue` / `attention_loss` を各 `-0.1`
- **保留理由**: 実地データを用いた妥当性調整を未実施
- **将来検討**:
  - 文脈依存（天候悪化時は減衰を弱める等）
  - `human_avg` 直接補正との比較

---

## 3. ドキュメント運用の未実装

### 3-1. 方針変更の自動同期

- **未実装理由**: `design` / `guide` / `presentation` の同期は現状手運用
- **将来検討**:
  - 変更時チェックリスト
  - lint 的な整合チェック（リンク・章番号・用語）

---

## 4. 参照

- `docs/design/rp_ui_and_simulation_vnext.md`
- `docs/design/rp_simulation.md`
- `apps/demo_web/risk_simulator.py`
- `apps/demo_web/static/guide/llm-role.html`
